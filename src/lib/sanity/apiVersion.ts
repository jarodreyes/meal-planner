/** Sanity API version (date-based). Use for GROQ, documents, and standard API. */
export const apiVersion = "2024-12-01";

/**
 * API version for Agent Actions (generate, transform, etc.).
 * @see https://www.sanity.io/docs/http-reference/agent-actions
 */
export const agentActionsApiVersion = "vX";
